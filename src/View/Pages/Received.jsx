import Page from "../Components/Page";
import Toolbar from "../Components/Toolbar";

const Received = () => {
  var toolbar = <Toolbar title="Received"></Toolbar>;

  return <Page toolbar={toolbar}></Page>;
};

export default Received;
