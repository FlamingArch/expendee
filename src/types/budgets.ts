export type Budget = {
  id: string;
  budgetAmount: number;
  categoryLabel: string;
  categoryIcon: string;
  color: string;
  currency: string;
  spentAmount: number;
  transactions: string[];
  userId: string;
};
